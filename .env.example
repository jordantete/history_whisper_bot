# Copier en .env (jamais commité). Secrets runtime + cible de déploiement.

# --- Runtime (lus par src/main.py via python-dotenv) ---
TELEGRAM_BOT_TOKEN=123456:ABC-your-bot-token
# OWNER_CHAT_ID=123456789   # (optionnel) chat qui reçoit les /feedback des utilisateurs
# SUBSCRIBERS_FILE=subscribers.json   # (optionnel) chemin du fichier des abonnés à l'envoi quotidien

# --- Déploiement (lus par scripts/deploy.sh) ---
VPS_USER=root
VPS_HOST=203.0.113.10
VPS_BOT_PATH=/root/history_whisper_bot
SSH_KEY=~/.ssh/id_ed25519
